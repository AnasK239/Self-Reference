Types of variables :

1- primitive = a normal variable which takes a set value

2- Reference = memory address (stack) points to (heap) 

## concatenation is acceptable in sout     ( you can do it with variables or strings whatever )

						syntax  sout("Hi" + X) (X is a variable or string)

## import java.util.scanner;            ( scanner is cin>>)

 						syntax  Scanner <name> = new Scanner(System.in); ## scans for input
EX:    

			 Scanner scanner = new Scanner(System.in);
        
       		 System.out.println("Enter your name");
        
       		 string name = scanner.nextLine();

        		 scanner.close();

## Important note : The .nextLine(); method reads the string including the spaces if you dont want spaces use !!! .next(); !!!

Remmeber to close the scanner using .close    // You need to add the Scanner.nextLine(); for the scanner to behave correctly and read input.

.nextLine(); >>>>> for string input  .nextInt(); for Integer input  etc ....


## A problem arises when accepting strings after integers because when you hit enter it picks up on the \n and thinks its the input
To clear the input buffer do:
					Int age = scanner.nextInt()
					scanner.nextLine();
		
					# then scan for your string
