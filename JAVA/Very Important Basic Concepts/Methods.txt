Putting a variable or a scanner or whatever after the public class main

makes it accesible to any method below


arrays 

String[] fruit = {" "," ", etc.. }

.length()  returns array length

for each loop

for(String c : (arrayname) ){
	
	sout << (c)
}


Arrays.sort(arrayname);

Arrays.fill (arrayname, value)    (fills all places with the variable)

